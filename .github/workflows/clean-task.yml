name: clean task
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  call_clean:
    runs-on: ubuntu-latest
    steps:
      - name: call api
        run: |
          curl -X POST "${{ secrets.FUNC_URL }}" -H "Authorization: ${{ secrets.FUNC_AUTH }}"
